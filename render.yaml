services:
  - type: web
    name: face-reader-backend
    runtime: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: REVENUECAT_API_KEY
        sync: false
